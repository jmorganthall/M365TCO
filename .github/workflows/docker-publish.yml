name: Publish container image

# Builds the multi-stage image (React UI + FastAPI) and publishes it to GitHub
# Container Registry so docker-compose.yml can pull ghcr.io/<owner>/m365tco:latest.
#
# `:latest` tracks the repository's DEFAULT branch (see the metadata step). List
# both `main` and the current default branch here because a push trigger's
# branch filter can't be an expression — the default-branch check that gates the
# `latest` tag is what actually keeps `:latest` correct if the default changes.
on:
  push:
    branches: [main, claude/m365-tco-tool-j39gf2]
    tags: ['v*.*.*']
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          # metadata-action lowercases the image name, so M365TCO -> m365tco.
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable=${{ github.ref_name == github.event.repository.default_branch }}
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,format=short

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          # Bake build provenance so the running container can offer an
          # "update available" notice. version is set only for v* tag builds.
          build-args: |
            TCO_BUILD_SHA=${{ github.sha }}
            TCO_BUILD_REF=${{ github.ref_name }}
            TCO_BUILD_VERSION=${{ steps.meta.outputs.version }}
